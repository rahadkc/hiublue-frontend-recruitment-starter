// Routes

export const ROUTES = {
  dashboard: '/dashboard',
  onboard: '/onboard',
} as const;

export const ENDPOINTS = {
  auth: '/login',
  summary: '/dashboard/summary',
  stat: '/dashboard/stat',
  offer: '/offers',
  users: '/users',
} as const;
